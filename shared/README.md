# Shared Package

`shared/` hosts cross-module utilities used by multiple FeCIM modules.

## Typical contents
- CLI/config loading helpers
- logging abstractions
- physics/material utility code
- shared widgets/themes/assets

Use this package for reusable infrastructure to avoid duplicating logic across modules.
