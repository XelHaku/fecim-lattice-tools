# M1–M4 Claim Matrix (Code-Verified)

Scope: `<local-path>`

Status legend:
- **TRUE**: implemented and directly evidenced in code/tests
- **PARTIAL**: implemented in limited form, or key parts missing/mismatched
- **FALSE**: not implemented as claimed
- **DEFERRED**: intentionally out of current module scope; tracked as future work

## Module 1 Claims

| # | Claim | Status | Evidence (code) | Test file(s) | Gap note |
|---|---|---|---|---|---|
| 1 | Preisach + Landau-Khalatnikov runtime switch | **TRUE** | `module1-hysteresis/pkg/gui/physics_engine.go` (`setPhysicsEngine`, `PhysicsLandau`, `PhysicsPreisach`); `module1-hysteresis/pkg/gui/controls.go` (engine selector) | `module1-hysteresis/pkg/gui/ui_sync_test.go`; controller validation in `module1-hysteresis/pkg/controller/ispp_convergence_test.go` | Runtime switch exists in GUI path. |
| 2 | ISPP achieves 30 discrete levels | **TRUE** | `module1-hysteresis/pkg/gui/gui.go` (`numLevels := 30`); `module1-hysteresis/pkg/controller/writer.go` (ISPP controller); level mapping in `module1-hysteresis/pkg/controller/ispp_convergence_test.go` | `module1-hysteresis/pkg/controller/ispp_full_cycle_test.go` (`TestISPPFullCycle_All30Levels_ResearchLikeBehavior`) | Achieved in Preisach path; LK single-domain is binary-limited (documented in tests). |
| 3 | ISPP average under 20 pulses per level | **TRUE** | Pulse accounting in `module1-hysteresis/pkg/controller/writer.go` (`PulseCount`, `TotalPulses`) | `module1-hysteresis/pkg/controller/ispp_full_cycle_test.go` (asserts `avgPulsesPerLevel < 20.0`) | Bound is explicitly tested. |
| 4 | WRD cycle: RESET→WRITE(ISPP)→READ phases with state machine | **PARTIAL** | WRD phase/state logic in `module1-hysteresis/pkg/gui/simulation.go` (phase mapping + controller states); ISPP state machine in `module1-hysteresis/pkg/controller/writer.go` | `module1-hysteresis/pkg/controller/ispp_full_cycle_test.go`; `module1-hysteresis/pkg/controller/writer_test.go` | Implemented, but comments/code note HOLD/VERIFY handled internally and some legacy phases removed/repurposed (not a strict external RESET→WRITE→READ state machine at all times). |
| 5 | 9 material presets (HZO variants, PZT, BTO, AlScN) | **DEFERRED** | Material constructors in `shared/physics/material.go` + `module1-hysteresis/pkg/ferroelectric/material.go`; config presets in `config/materials.yaml` include HZO-family + AlScN (+In2Se3), but no PZT/BTO implementations | `shared/physics/material_test.go`; `shared/physics/material_methods_test.go` | Limitation documented: current project scope prioritizes HZO-family/AlScN FeCIM materials; adding PZT/BTO requires new calibrated material parameter sets and validation data before claiming parity. |
| 6 | Temperature-dependent Ec/Pr (Curie-Weiss) | **PARTIAL** | `shared/physics/material.go` (`CoerciveFieldAtTemp`, `PolarizationAtTemp`) uses `~(1-T/Tc)^0.5` scaling | `shared/physics/material_test.go`; `shared/physics/edge_cases_test.go`; `module1-hysteresis/pkg/ferroelectric/physics_validation_test.go` | Temperature dependence exists, but model is implemented as sqrt critical scaling, not a full Curie-Weiss law statement as claimed. |
| 7 | Endurance/fatigue model (wake-up→stable→fatigue) | **PARTIAL** | Fatigue degradation model: `shared/physics/material.go` (`EnduranceAtCycles`), `shared/physics/write_verify_stats.go`; GUI phase label logic in `module1-hysteresis/pkg/gui/simulation.go` | `module1-hysteresis/pkg/ferroelectric/cycling_effects_test.go`; `shared/physics/material_test.go` | Fatigue/endurance is modeled; explicit wake-up/stable/fatigue progression in GUI currently uses placeholder values in simulation loop. |
| 8 | Equation modal with SVG rendering | **TRUE** | Modal wiring in `module1-hysteresis/pkg/gui/equation_dialog.go`; SVG pipeline/cache in `module1-hysteresis/pkg/gui/widgets/physics_equations.go` | `module1-hysteresis/pkg/gui/equation_dialog_test.go`; `module1-hysteresis/pkg/gui/widgets/physics_equations_asset_test.go`; `physics_equations_pipeline_test.go` | Implemented with SVG + fallback behavior tests. |
| 9 | CSV/JSON data export | **TRUE** | `module1-hysteresis/pkg/gui/export.go` (JSON+CSV export), `module1-hysteresis/pkg/gui/data_logger.go` (CSV logger), CLI JSON in `module1-hysteresis/cmd/hysteresis/main.go` | `module1-hysteresis/pkg/gui/keyboard.go` shortcut wiring; export path exercised in GUI tests indirectly | Export code is present and wired. |

## Module 4 Claims

| # | Claim | Status | Evidence (code) | Test file(s) | Gap note |
|---|---|---|---|---|---|
| 10 | WRITE/READ/COMPUTE operations on NxN crossbar (up to 128×128) | **TRUE** | Modes in `module4-circuits/pkg/gui/device_state.go` (`OpModeRead/Write/Compute`); max constant in `module4-circuits/pkg/gui/app.go` (`MaxArraySize=128`); size selector now includes 128 in `module4-circuits/pkg/gui/tab_unified.go` (`ValidArraySizes`) | `module4-circuits/pkg/gui/device_state_test.go`; `module4-circuits/pkg/gui/unified_buttons_test.go`; `module4-circuits/pkg/gui/tab_unified_array_size_test.go` | UI and backend both support 128×128. |
| 11 | Architecture selection: 0T1R/1T1R/2T1R | **TRUE** | Architecture toggles and behavior in `module4-circuits/pkg/gui/tab_unified.go`, `device_state.go`, `tab_unified_voltage.go` | `module4-circuits/pkg/gui/tab_unified_extended_test.go`; `module4-circuits/pkg/gui/unified_buttons_test.go`; `module4-circuits/pkg/gui/physics_test.go` | Fully implemented in UI/state logic. |
| 12 | DAC with configurable bit resolution (4/5/6/8-bit), DNL/INL ±1 LSB | **TRUE** | DAC model has INL/DNL in `shared/peripherals/dac.go`; Module 4 UI DAC selector in `module4-circuits/pkg/gui/tab_unified.go`; wiring in `module4-circuits/pkg/gui/device_state.go` (`SetDACBits`, DAC recreation) | `shared/peripherals/defaults_test.go`; `shared/peripherals/process_corner_test.go`; `module4-circuits/pkg/gui/tab_unified_array_size_test.go` | DAC bit configurability is now exposed in operations UI (4–8 bit options). |
| 13 | Charge pump voltage boosting | **PARTIAL** | Charge pump model in `shared/peripherals/chargepump.go`; integrated peripheral chain/spice references in `module4-circuits/pkg/arraysim/spice_export.go` | `shared/peripherals/peripherals_test.go`; `shared/peripherals/new_peripherals_test.go` | Model exists, but no strong evidence of direct interactive Module 4 UI control path for charge pump boosting in main flow. |
| 14 | TIA with configurable gain | **TRUE** | TIA model in `shared/peripherals/tia.go`; UI control and preset application in `module4-circuits/pkg/gui/tab_unified.go` (`applySenseRf`, presets) | `module4-circuits/pkg/gui/tab_unified_extended_test.go`; `shared/peripherals/peripherals_test.go` | Implemented and tested. |
| 15 | ADC with matching resolution | **PARTIAL** | ADC model in `shared/peripherals/adc.go`; selectable ADC bits in `module4-circuits/pkg/gui/tab_unified.go` | `module4-circuits/pkg/gui/physics_test.go` (`TestADCBitResolution`) | ADC resolution is configurable (5–8 bits), but “matching” DAC resolution is not clearly enforced because DAC bit control is not equivalently exposed. |
| 16 | Noise models: thermal, 1/f, shot, quantization + SNR | **TRUE** | Production noise helpers in `shared/peripherals/noise.go` (`ThermalNoiseRMS`, `FlickerNoisePower`, `ShotNoiseCurrentRMS`, `QuantizationNoiseVariance`, `TotalNoiseVariance`, `SNRDB`); SNR chain consumption in `module4-circuits/pkg/gui/tab_unified.go` | `shared/peripherals/noise_test.go`; `shared/peripherals/peripherals_test.go` | Noise primitives are now available in production package and consumable from simulation chain. |
| 17 | Half-select disturb tracking | **TRUE** | V/2 and disturb accumulation in `module4-circuits/pkg/gui/tab_unified_voltage.go` (`halfSelectResidue`, disturb update); half-select state in `device_state.go` | `module4-circuits/pkg/gui/tab_unified_halfselect_voltage_test.go`; `module4-circuits/pkg/gui/physics_test.go` | Implemented with explicit tracking and tests. |
| 18 | Per-cell signed voltage/current display with toggle | **TRUE** | Cell info toggle in `module4-circuits/pkg/gui/tab_unified.go` (`sharedCellDisplayToggle`) now switches signed per-cell display field between `V_cell (V)` and `I_cell (µA)` using `%+` formatting; `updateCellInfo` computes signed `I = G × V` | `module4-circuits/pkg/gui/tab_unified_extended_test.go` (`TestUnifiedTabCellInfoSignedToggle`) | Implemented in operations view; toggle now changes displayed signed metric mode deterministically. |
| 19 | Comparison view (FeCIM vs SRAM/ReRAM/MRAM benchmarks) | **DEFERRED** | Module 4 comparison tab (`module4-circuits/pkg/gui/tab_comparison.go`) is scoped to peripheral-circuits comparison (CPU/GPU/FeFET path), not multi-technology memory benchmarking | `module4-circuits/pkg/gui/tab_unified_extended_test.go` (comparison canvas presence) | Module 5 scope: SRAM/ReRAM/MRAM comparison belongs in `module5-comparison`; keep out of M1–M4 compliance claims to avoid cross-module scope drift. |
| 20 | Reference view (timing diagrams + spec tables) | **TRUE** | Reference tab composition in `module4-circuits/pkg/gui/tab_reference.go`; timing/spec sections in `tab_reference_timing.go`, `tab_reference_specs.go`, `tab_reference_voltage.go` | `module4-circuits/pkg/gui/tab_unified_extended_test.go` (reference subview presence/selectors) | Implemented as dedicated reference views. |
| 21 | Shared physics library — same Preisach code in both modules | **TRUE** | Module 1 Preisach wrapper in `module1-hysteresis/pkg/ferroelectric/preisach.go` directly delegates stack dynamics to `shared/physics.PreisachStack` (`stack *physics.PreisachStack`, `physics.NewPreisachStack`, `stack.Update`, `stack.ComputePolarization`); Module 4 uses shared physics models in its device path (`module4-circuits/pkg/gui/device_state.go`) | `module4-circuits/pkg/gui/module1_module4_integration_test.go` | Module 1 keeps a thin wrapper for module-local APIs (temperature/stress/reversible terms), but irreversible Preisach engine is shared from `shared/physics`. |

## Notes

- This matrix is based on direct code and test inspection, not on README/marketing text.
- Several claims are directionally true but overstate UI exposure or strictness of implementation details; those are marked **PARTIAL**.
