# Citation Paper Records

Markdown files below this directory are citation source records keyed by filename.
The public lookup contract is the paper key, not a fixed one-level path: tooling should locate records recursively under `citations/papers/`.

Shared locator contract for repository scripts: `scripts/citations/_paper_records.sh`.
